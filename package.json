{
  "name": "advanced-tab-manager",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "tsc --noEmit",
    "lint": "node scripts/build.mjs && node scripts/validate.mjs",
    "test": "node scripts/build.mjs && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.9.0"
  }
}
