{
  "name": "advanced-tab-manager",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "tsc --noEmit",
    "lint": "node scripts/build.mjs && web-ext lint --source-dir dist",
    "test": "node scripts/build.mjs && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/firefox-webext-browser": "^120.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.9.0",
    "web-ext": "^10.6.0"
  }
}
